class Release

	instance variables
		public name: Utils`String := [];
		public timestamp: nat; -- TODO change Type
	
	operations
		public Release: Utils`String ==> Release
		Release(n) == (
			name := n;
			return self;
		)
		pre n <> [];
		
end Release